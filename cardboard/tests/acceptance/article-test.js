import { module, skip } from 'qunit';
module('Acceptance | article', function(/*hooks*/) {

  skip('TODO when a published article is unpublished it sets the readers field correctly', async function(/*assert*/) {
  });

  skip('TODO when an unpublished article is published it sets the readers field correctly', async function(/*assert*/) {
  });

  skip('TODO when an unpublished article is published it sets the published date correctly', async function(/*assert*/) {
  });

});